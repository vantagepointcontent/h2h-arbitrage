const eligible = 32594;
console.log(JSON.stringify({
  profitPct: (eligible - 1751) * 100 / eligible,
  apyPctAll: (eligible - 1682) * 100 / eligible,
  stakePct: (eligible - 615) * 100 / eligible,
}));
