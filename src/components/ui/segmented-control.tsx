import { cn } from '@/lib/utils';
export function SegmentedControl<T extends string>({value,options,onChange,ariaLabel,className}:{value:T;options:readonly {value:T;label:string;disabled?:boolean}[];onChange:(value:T)=>void;ariaLabel:string;className?:string}){return <div role="group" aria-label={ariaLabel} className={cn('inline-flex rounded-[var(--radius-control)] border border-[var(--border-strong)] bg-[var(--surface-raised)] p-0.5',className)}>{options.map(option=><button key={option.value} type="button" disabled={option.disabled} aria-pressed={value===option.value} onClick={()=>onChange(option.value)} className={cn('min-h-8 rounded-[calc(var(--radius-control)-2px)] px-2.5 text-xs font-medium text-[var(--text-secondary)] transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--focus-ring)] disabled:opacity-40',value===option.value&&'bg-[var(--surface-selected)] text-[var(--status-positive)]')}>{option.label}</button>)}</div>}
