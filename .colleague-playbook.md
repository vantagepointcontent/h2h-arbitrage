H2H Dashboard Recovery — Colleague Playbook
═══════════════════════════════════════════════════════════════

CONTEXT
-------
The Hermes dashboard at http://100.86.7.30:9119 shows:
  "Failed to load Kanban board: 500 Internal Server Error"
Additionally:
  • Next.js dev server is down (curl to :3000 fails).
  • TypeScript reports one error in src/app/page.tsx line 1635 ( TS1005 "')' expected" ).
  • The file is 2,338 lines long; the error has survived both git-checkout and cache purges,
    which means the fix must be applied IN the file AND the cached build artifacts removed.

DO NOT ASK VICTOR TO TYPE "kör" — EXECUTE EVERYTHING BELOW IN SEQUENCE.

───────────────────────────────────────────────────────────────
STEP 1 — SSH / Terminal preparation
───────────────────────────────────────────────────────────────
Connect to the same host Victor uses (100.86.7.30) and become user scott.

───────────────────────────────────────────────────────────────
STEP 2 — Kill stale Next.js / Node processes
───────────────────────────────────────────────────────────────
Run:

  cd ~/h2h-arbitrage
  pkill -f "next dev" || true
  pkill -f "node.*next" || true
  sleep 2

Confirm no port collisions remain:

  ss -tlnp | grep -E ':3000|:3005'

(If anything is still listening, note the PID and kill it with kill -9.)

───────────────────────────────────────────────────────────────
STEP 3 — Evade the Turbopack cache loop (critical)
───────────────────────────────────────────────────────────────
Run:

  rm -rf .next/
  rm -rf node_modules/.cache/
  rm -f tsconfig.tsbuildinfo
  rm -rf /tmp/turbopack-*

───────────────────────────────────────────────────────────────
STEP 4 — Fix the TypeScript error manually
───────────────────────────────────────────────────────────────
Open src/app/page.tsx in your editor.

Navigate to line 1635.
The line currently reads:

                          </tbody>

The prior line (1634) ends a map IIFE. It currently shows:

                            })}

Change line 1634 FROM:
                            })
TO:
                            })()

Then immediately after the closing </tbody> (line 1635), verify the next lines read exactly:

                          </tbody>
                        </table>
                      </div>
                    )}

No extra closing brace may be inserted here.

───────────────────────────────────────────────────────────────
STEP 5 — Verify TypeScript compiles
───────────────────────────────────────────────────────────────
Run:

  npx tsc --noEmit

Expected result: zero errors.
If any error remains, STOP and paste the full output to Victor.

───────────────────────────────────────────────────────────────
STEP 6 — Start Next.js dev server (STABLE mode, no --turbo)
───────────────────────────────────────────────────────────────
Run:

  npm run dev

Watch the terminal for the port (usually 3000).
Wait until you see:

  Ready in Xs
  - Local:        http://localhost:3000

Leave this terminal running. Open a NEW terminal for the next steps.

───────────────────────────────────────────────────────────────
STEP 7 — Restart Hermes gateway so it binds 9119
───────────────────────────────────────────────────────────────
In a fresh terminal, run EXACTLY:

  hermes gateway stop
  sleep 2
  hermes gateway status

If it says "service is NOT running", start it with:

  hermes gateway run --replace

Wait 5 seconds, then verify it listens on port 9119:

  curl -s -o /dev/null -w "%{http_code}" http://localhost:9119/sessions

Expected: 200. Any other value means the gateway isn't bound yet — check
`hermes gateway status` for the correct PID.

───────────────────────────────────────────────────────────────
STEP 8 — Verify both services from outside
───────────────────────────────────────────────────────────────
On your local machine (NOT the server), run:

  curl http://100.86.7.30:3000
  curl http://100.86.7.30:9119/sessions

Both should return HTTP 200 (not 000 or 500).

───────────────────────────────────────────────────────────────
DONE
───────────────────────────────────────────────────────────────
Reply to Victor with the output of:
  - "npx tsc --noEmit" (should say 0 errors)
  - "curl -w %{http_code} http://100.86.7.30:3000" (should say 200)
  - "curl -w %{http_code} http://100.86.7.30:9119/sessions" (should say 200)

DO NOT STOP HALF-WAY. DO NOT ASK FOR "kör". EXECUTE ALL STEPS IN ORDER.
